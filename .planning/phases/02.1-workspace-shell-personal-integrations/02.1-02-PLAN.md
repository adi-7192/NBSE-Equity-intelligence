---
phase: 02.1-workspace-shell-personal-integrations
plan: 02
type: execute
wave: 2
depends_on:
  - "02.1-01"
files_modified:
  - lib/db/schema.ts
  - lib/db/queries/user-integrations.ts
  - lib/security/encryption.ts
  - app/settings/page.tsx
  - app/page.tsx
  - app/admin/page.tsx
  - components/settings/profile-panel.tsx
  - components/settings/integrations-panel.tsx
  - components/settings/security-panel.tsx
  - components/settings/workspace-status-panel.tsx
  - components/integrations/provider-status-card.tsx
  - components/integrations/connect-empty-state.tsx
  - .env.example
autonomous: true
requirements:
  - INTG-01
  - INTG-02
  - SHELL-02
user_setup:
  - Add a stable `APP_ENCRYPTION_KEY` environment variable for server-side secret encryption before using saved personal integrations in production.
must_haves:
  truths:
    - Each signed-in user can manage personal provider settings for Zerodha, OpenAI, Gemini, and Anthropic from Settings.
    - Provider credentials are stored encrypted server-side and are never returned raw to the frontend after save.
    - Users see clear configured/unconfigured/status states and setup guidance instead of broken feature surfaces.
  artifacts:
    - A durable per-user integrations table and query layer exist.
    - A server-side encryption utility exists and is used before persistence.
    - Settings renders dedicated Profile, Integrations, Security, and Workspace Status sections with provider cards.
  key_links:
    - The settings UI writes through authenticated server actions to the per-user integration storage layer.
    - Dashboard and related surfaces can detect missing integrations and show designed empty states.
    - Saved provider records expose masked metadata only, not raw secrets.
---

<objective>
Implement secure personal integration settings and capability-aware UX.

Purpose: Make Settings real by adding protected per-user provider storage, integration management panels, and graceful product behavior when connections are missing.
Output: Encrypted per-user integration persistence, settings sections, provider cards, and integration-aware empty/status states.
</objective>

<execution_context>
@/Users/adi7192/Documents/Equity Intelligence/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adi7192/Documents/Equity Intelligence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-workspace-shell-personal-integrations/02.1-CONTEXT.md
@.planning/phases/02.1-workspace-shell-personal-integrations/02.1-RESEARCH.md
@.planning/phases/02.1-workspace-shell-personal-integrations/02.1-UI-SPEC.md
@.planning/phases/02.1-workspace-shell-personal-integrations/02.1-01-PLAN.md
@lib/auth.ts
@lib/db/schema.ts
@app/settings/page.tsx
@app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add encrypted per-user integration storage and server-only helpers</name>
  <files>lib/db/schema.ts, lib/db/queries/user-integrations.ts, lib/security/encryption.ts, .env.example</files>
  <read_first>lib/db/schema.ts, lib/auth.ts, .planning/phases/02.1-workspace-shell-personal-integrations/02.1-CONTEXT.md, .planning/phases/02.1-workspace-shell-personal-integrations/02.1-RESEARCH.md</read_first>
  <action>Create a durable per-user integration storage model in Postgres for Zerodha, OpenAI, Gemini, and Anthropic, add a server-only encryption helper using a dedicated `APP_ENCRYPTION_KEY`, and expose query/helpers that save encrypted secrets plus masked metadata such as provider, label, configured state, validation state, and timestamps. The query layer must never return raw decrypted values to client components once data has been saved.</action>
  <verify>test -f lib/security/encryption.ts && test -f lib/db/queries/user-integrations.ts && rg "APP_ENCRYPTION_KEY|encrypt|decrypt|provider|configured|lastValidatedAt" lib/security/encryption.ts lib/db/queries/user-integrations.ts lib/db/schema.ts .env.example</verify>
  <acceptance_criteria>
    - A per-user integrations schema exists in `lib/db/schema.ts`
    - `lib/security/encryption.ts` exists and performs authenticated encryption/decryption server-side
    - `.env.example` documents `APP_ENCRYPTION_KEY`
    - Saved integration metadata can be listed without exposing raw secrets
  </acceptance_criteria>
  <done>The app has a secure persistence foundation for personal provider credentials.</done>
</task>

<task type="auto">
  <name>Task 2: Build the settings page sections and provider management UX</name>
  <files>app/settings/page.tsx, components/settings/profile-panel.tsx, components/settings/integrations-panel.tsx, components/settings/security-panel.tsx, components/settings/workspace-status-panel.tsx, components/integrations/provider-status-card.tsx, components/integrations/connect-empty-state.tsx</files>
  <read_first>app/settings/page.tsx, .planning/phases/02.1-workspace-shell-personal-integrations/02.1-UI-SPEC.md, .planning/phases/02.1-workspace-shell-personal-integrations/02.1-CONTEXT.md</read_first>
  <action>Implement the dedicated Settings page with secondary left sub-navigation for Profile, Integrations, Security, and Workspace Status. Under Integrations, add first-class provider cards for Zerodha, OpenAI, Gemini, and Anthropic that support save/update/disconnect flows, show masked configured states and timestamps, and keep copy calm and operator-like. Zerodha should use a friendly minimal-input form, and AI providers should use a consistent card/form model rather than four unrelated layouts.</action>
  <verify>test -f components/settings/integrations-panel.tsx && test -f components/integrations/provider-status-card.tsx && rg "Profile|Integrations|Security|Workspace Status|Zerodha|OpenAI|Gemini|Anthropic" app/settings/page.tsx components/settings/integrations-panel.tsx</verify>
  <acceptance_criteria>
    - Settings has four top-level sections with a secondary nav
    - Integrations includes Zerodha, OpenAI, Gemini, and Anthropic cards
    - Saved states show masked/status metadata rather than raw credentials
    - Provider forms share a coherent visual and interaction pattern
  </acceptance_criteria>
  <done>Users can manage personal integrations from a first-class settings experience.</done>
</task>

<task type="auto">
  <name>Task 3: Add capability-aware empty states and shell-level integration status cues</name>
  <files>app/page.tsx, app/admin/page.tsx, app/settings/page.tsx, components/integrations/connect-empty-state.tsx</files>
  <read_first>app/page.tsx, app/admin/page.tsx, .planning/phases/02.1-workspace-shell-personal-integrations/02.1-CONTEXT.md</read_first>
  <action>Update the dashboard and related authenticated surfaces so missing integrations produce polished setup guidance instead of broken widgets. Make Settings the obvious recovery path, and add lightweight shell or page-level status cues for unconfigured providers without hard-blocking the whole app. Only block actions that genuinely require the missing provider.</action>
  <verify>rg "Connect Zerodha|Connect an AI provider|Settings" app/page.tsx app/settings/page.tsx app/admin/page.tsx components/integrations/connect-empty-state.tsx</verify>
  <acceptance_criteria>
    - Dashboard can render meaningful empty states when Zerodha or AI are not configured
    - Settings is the clear destination to resolve missing integrations
    - The app does not hard-block navigation because a provider is unconfigured
  </acceptance_criteria>
  <done>The product behaves gracefully when integrations are missing and guides users toward setup.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npx tsc --noEmit` passes
- [ ] A signed-in user can save and update provider settings without raw secrets being re-rendered
- [ ] Saved provider records appear masked/status-only in the UI
- [ ] Dashboard and settings show polished setup guidance when providers are missing
- [ ] Admin and non-admin users only see their own integration settings
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Personal integrations are securely stored and usable as a real product capability
- The shell and settings UX now support future live-market and AI features without relying on hidden env-only configuration
  </success_criteria>

<output>
After completion, create `.planning/phases/02.1-workspace-shell-personal-integrations/02.1-02-SUMMARY.md`
</output>
