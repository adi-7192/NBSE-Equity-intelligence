---
phase: 02.1-workspace-shell-personal-integrations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/globals.css
  - app/page.tsx
  - app/archives/page.tsx
  - app/admin/page.tsx
  - app/settings/page.tsx
  - app/screener/page.tsx
  - app/portfolio/page.tsx
  - components/session-toolbar.tsx
  - components/dashboard-client.tsx
  - components/market-header.tsx
  - components/app-shell.tsx
  - components/app-sidebar.tsx
  - components/app-topbar.tsx
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
user_setup: []
must_haves:
  truths:
    - Signed-in users work inside one coherent authenticated shell instead of page-specific headers.
    - Navigation clearly exposes Dashboard, Archives, Screener, Portfolio, Settings, and role-aware Admin access.
    - The dark theme, typography, spacing, and empty/loading states feel intentionally designed rather than improvised.
  artifacts:
    - A reusable shell component exists and wraps authenticated routes.
    - Settings, Screener, and Portfolio routes exist inside the shell, even if some are placeholders.
    - The dashboard and archives surfaces visually align with the new shell and information hierarchy.
  key_links:
    - The shell respects the existing auth/admin boundary and changes nav visibility by role.
    - App-level layout, sidebar, top bar, and page sections share one typography and spacing contract.
---

<objective>
Build the reusable workspace shell and visual system for Phase 02.1.

Purpose: Turn the current collection of standalone pages into a coherent private terminal with durable navigation, stronger dark-theme UI, and future-ready route structure.
Output: Shared authenticated shell, improved typography/theme/layout system, and integrated Dashboard/Archives/Admin/Settings/Screener/Portfolio navigation.
</objective>

<execution_context>
@/Users/adi7192/Documents/Equity Intelligence/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adi7192/Documents/Equity Intelligence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-workspace-shell-personal-integrations/02.1-CONTEXT.md
@.planning/phases/02.1-workspace-shell-personal-integrations/02.1-RESEARCH.md
@.planning/phases/02.1-workspace-shell-personal-integrations/02.1-UI-SPEC.md
@app/layout.tsx
@app/page.tsx
@app/archives/page.tsx
@app/admin/page.tsx
@components/dashboard-client.tsx
@components/session-toolbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce a reusable authenticated shell and route structure</name>
  <files>app/layout.tsx, app/page.tsx, app/archives/page.tsx, app/admin/page.tsx, app/settings/page.tsx, app/screener/page.tsx, app/portfolio/page.tsx, components/app-shell.tsx, components/app-sidebar.tsx, components/app-topbar.tsx</files>
  <read_first>app/layout.tsx, app/page.tsx, app/archives/page.tsx, app/admin/page.tsx, .planning/phases/02.1-workspace-shell-personal-integrations/02.1-CONTEXT.md, .planning/phases/02.1-workspace-shell-personal-integrations/02.1-UI-SPEC.md</read_first>
  <action>Create a reusable authenticated shell with a left sidebar and slim top bar, move signed-in product pages into that frame, make the primary nav include Dashboard, Archives, Screener, Portfolio, Settings, and Admin only for admin users, and add polished in-shell placeholder routes for Screener and Portfolio so the information architecture is visible immediately. Replace the current standalone `SessionToolbar` usage with the new shell and make sure `app/page.tsx`, `app/archives/page.tsx`, and `app/admin/page.tsx` all render inside the shared layout model.</action>
  <verify>test -f components/app-shell.tsx && test -f components/app-sidebar.tsx && test -f components/app-topbar.tsx && test -f app/settings/page.tsx && test -f app/screener/page.tsx && test -f app/portfolio/page.tsx && rg "Dashboard|Archives|Screener|Portfolio|Settings|Admin" components/app-sidebar.tsx</verify>
  <acceptance_criteria>
    - A shared shell component exists and is used by authenticated routes
    - Sidebar includes Dashboard, Archives, Screener, Portfolio, Settings, and role-aware Admin
    - Screener and Portfolio routes exist as polished placeholders inside the shell
    - Admin remains hidden for non-admin users
  </acceptance_criteria>
  <done>The app has a real workspace shell and future-ready navigation structure.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the terminal-finance visual system across the main app surfaces</name>
  <files>app/globals.css, app/layout.tsx, components/dashboard-client.tsx, components/market-header.tsx, app/archives/page.tsx, components/session-toolbar.tsx</files>
  <read_first>app/globals.css, app/layout.tsx, components/dashboard-client.tsx, .planning/phases/02.1-workspace-shell-personal-integrations/02.1-UI-SPEC.md</read_first>
  <action>Upgrade the global typography and theme tokens to match the approved UI contract, replace the current body font with a more expressive finance-terminal friendly face, strengthen spacing rhythm and alignment across dashboard/archive surfaces, and refactor page framing so cards, status panels, headers, and empty states look intentional in the new shell. Keep live-market semantics intact by reserving green/red for price and health signals.</action>
  <verify>rg "font-manrope|Manrope|font-jetbrains|sidebar|amber|bullish|bearish" app/layout.tsx app/globals.css && rg "Weekly Intelligence|Live Market" components/dashboard-client.tsx components/market-header.tsx</verify>
  <acceptance_criteria>
    - Global typography uses the new body/display hierarchy and keeps mono moments for market metadata
    - Dashboard and archives no longer look like standalone pages with mismatched spacing/alignment
    - Theme tokens reflect the charcoal/graphite plus amber contract
    - Loading/empty/state framing is visibly more deliberate than the current baseline
  </acceptance_criteria>
  <done>The product has a stronger, more cohesive dark terminal-style UI foundation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npx tsc --noEmit` passes
- [ ] Dashboard, Archives, Settings, Screener, and Portfolio all render inside the shared shell
- [ ] Admin nav item is visible only to admins
- [ ] The updated typography and spacing system is visible across the main authenticated surfaces
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The authenticated product feels like one application shell rather than separate pages
- The UI direction clearly matches the approved terminal-finance design contract
  </success_criteria>

<output>
After completion, create `.planning/phases/02.1-workspace-shell-personal-integrations/02.1-01-SUMMARY.md`
</output>
