---
phase: 01-foundation-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .env.example
  - drizzle.config.ts
  - lib/db/index.ts
  - lib/db/schema.ts
  - lib/db/queries/archive.ts
  - lib/archive.ts
  - app/page.tsx
  - app/api/update-data/route.ts
  - app/api/archives/route.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
user_setup:
  - Create a Supabase project and provide a working `DATABASE_URL` for local/dev and Vercel environments.
must_haves:
  truths:
    - Live dashboard state is loaded from PostgreSQL rather than module-level memory.
    - Intelligence archives persist across restarts and are stored as immutable records with metadata.
    - The existing dashboard update and archive routes continue to work through the new persistence layer.
  artifacts:
    - drizzle.config.ts exists and points at the project schema.
    - lib/db/index.ts exports a shared Drizzle/Postgres client.
    - lib/db/schema.ts defines the phase-one durable storage tables.
    - lib/db/queries/archive.ts contains the durable archive/live-state query helpers.
    - lib/archive.ts delegates to the database-backed implementation.
  key_links:
    - app/api/update-data/route.ts archives and saves through lib/archive.ts.
    - app/api/archives/route.ts reads through the durable archive query path.
    - app/page.tsx reads live dashboard state through the migrated persistence boundary.
---

<objective>
Establish the durable Phase 1 data foundation for the dashboard and archive system.

Purpose: Replace the current throwaway in-memory storage with a PostgreSQL-backed persistence layer that can survive deploys and support later user-scoped features.
Output: Database tooling, schema, archive query helpers, and migrated archive/live-state callers.
</objective>

<execution_context>
@/Users/adi7192/Documents/Equity Intelligence/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adi7192/Documents/Equity Intelligence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-access/01-CONTEXT.md
@.planning/phases/01-foundation-access/01-RESEARCH.md
@lib/archive.ts
@app/page.tsx
@app/api/update-data/route.ts
@app/api/archives/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add database tooling and environment scaffolding</name>
  <files>package.json, .env.example, drizzle.config.ts, lib/db/index.ts</files>
  <read_first>package.json, tsconfig.json, .planning/phases/01-foundation-access/01-CONTEXT.md, .planning/phases/01-foundation-access/01-RESEARCH.md</read_first>
  <action>Add the durable persistence dependencies `drizzle-orm` and `pg` to `package.json`, add the development dependencies `drizzle-kit` and `tsx`, create `.env.example` with an explicit `DATABASE_URL=` entry for the Supabase Postgres connection, create `drizzle.config.ts` that reads `DATABASE_URL`, uses `./lib/db/schema.ts` as the schema source, and writes migrations into `./drizzle`, and create `lib/db/index.ts` that exports both a shared `Pool` from `pg` and a Drizzle `db` client from `drizzle-orm/node-postgres`.</action>
  <verify>rg "drizzle-orm|drizzle-kit|pg" package.json && rg "^DATABASE_URL=" .env.example && test -f drizzle.config.ts && test -f lib/db/index.ts</verify>
  <acceptance_criteria>
    - `package.json` contains `drizzle-orm`, `pg`, `drizzle-kit`, and `tsx`
    - `.env.example` contains `DATABASE_URL=`
    - `drizzle.config.ts` contains `schema: "./lib/db/schema.ts"`
    - `lib/db/index.ts` contains `new Pool` and `drizzle(`
  </acceptance_criteria>
  <done>Database tooling and env scaffolding exist and are grep-verifiable.</done>
</task>

<task type="auto">
  <name>Task 2: Define the phase-one persistence schema and durable archive helpers</name>
  <files>lib/db/schema.ts, lib/db/queries/archive.ts</files>
  <read_first>lib/archive.ts, app/api/update-data/route.ts, app/api/archives/route.ts, .planning/REQUIREMENTS.md, .planning/phases/01-foundation-access/01-CONTEXT.md</read_first>
  <action>Create `lib/db/schema.ts` with Drizzle table definitions for `live_dashboard_state` and `intelligence_snapshots` as the actively used phase-one tables, plus minimal future-facing scaffolding tables for `stocks`, `watchlists`, `watchlist_items`, and `portfolio_holdings` so PostgreSQL becomes the system-of-record path for later phases. Include metadata columns such as `ownerUserId`, `updatedAt`, `archivedAt`, `sourceModel`, and a durable JSON payload field for dashboard/intelligence records. Create `lib/db/queries/archive.ts` with explicit helpers for loading the current dashboard state, saving the current dashboard state, creating immutable archive snapshots, listing archives, and retrieving one archive by filename or ID.</action>
  <verify>test -f lib/db/schema.ts && test -f lib/db/queries/archive.ts && rg "live_dashboard_state|intelligence_snapshots|stocks|watchlists|portfolio_holdings" lib/db/schema.ts && rg "save|archive|list|get" lib/db/queries/archive.ts</verify>
  <acceptance_criteria>
    - `lib/db/schema.ts` contains `live_dashboard_state` and `intelligence_snapshots`
    - `lib/db/schema.ts` contains `stocks`, `watchlists`, `watchlist_items`, and `portfolio_holdings`
    - `lib/db/schema.ts` contains `ownerUserId`, `updatedAt`, and `archivedAt`
    - `lib/db/queries/archive.ts` exports helpers for saving live state, archiving snapshots, listing archives, and retrieving a single archive
  </acceptance_criteria>
  <done>The database schema and archive query layer exist and match the phase-one persistence scope.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the existing archive boundary and route callers to durable storage</name>
  <files>lib/archive.ts, app/page.tsx, app/api/update-data/route.ts, app/api/archives/route.ts</files>
  <read_first>lib/archive.ts, lib/db/queries/archive.ts, app/page.tsx, app/api/update-data/route.ts, app/api/archives/route.ts, lib/data.ts</read_first>
  <action>Refactor `lib/archive.ts` so it keeps the existing exported function names `saveLiveData`, `getLiveData`, `archiveCurrentData`, `listArchives`, and `getArchive`, but replaces module-level state with calls into `lib/db/queries/archive.ts`. Update `app/page.tsx`, `app/api/update-data/route.ts`, and `app/api/archives/route.ts` so they continue using the same archive module boundary while persisting live state and snapshots through PostgreSQL. Ensure archived records include metadata such as `archivedAt` and `sourceModel` where available instead of saving anonymous blobs.</action>
  <verify>rg "export async function saveLiveData|export async function getLiveData|export async function archiveCurrentData|export async function listArchives|export async function getArchive" lib/archive.ts && rg "@/lib/archive" app/page.tsx app/api/update-data/route.ts app/api/archives/route.ts && rg "db|saveDashboardState|archiveSnapshot" lib/archive.ts</verify>
  <acceptance_criteria>
    - `lib/archive.ts` still exports the five existing archive functions by name
    - `lib/archive.ts` no longer contains `let currentLiveData` or `const archiveHistory`
    - `app/page.tsx`, `app/api/update-data/route.ts`, and `app/api/archives/route.ts` still import from `@/lib/archive`
    - The new archive path contains metadata fields for archive provenance
  </acceptance_criteria>
  <done>The dashboard/archive callers keep working through a database-backed storage layer.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `rg "let currentLiveData|const archiveHistory" lib/archive.ts` returns no matches
- [ ] `test -f lib/db/index.ts && test -f lib/db/schema.ts && test -f lib/db/queries/archive.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Durable archive and live-state persistence exist behind the current archive boundary
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-access/01-01-SUMMARY.md`
</output>
