---
phase: 01-foundation-access
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - package.json
  - .env.example
  - lib/auth.ts
  - lib/db/schema.ts
  - app/api/auth/[...all]/route.ts
  - middleware.ts
  - app/login/page.tsx
  - app/page.tsx
  - app/api/update-data/route.ts
  - app/api/archives/route.ts
  - lib/archive.ts
  - lib/db/queries/archive.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
user_setup:
  - Set `BETTER_AUTH_SECRET` and `BETTER_AUTH_URL` in local and Vercel environments, then bootstrap the initial admin account credentials.
must_haves:
  truths:
    - Approved users can sign in with email and password.
    - One authenticated admin user can create or invite the remaining approved accounts from inside the app.
    - Unauthenticated users cannot access the dashboard or private API routes.
    - Archive and live-dashboard reads/writes are scoped to the authenticated user.
  artifacts:
    - lib/auth.ts exists with Better Auth configuration and session helpers.
    - app/api/auth/[...all]/route.ts exists and exposes the auth handler.
    - middleware.ts protects private routes.
    - app/login/page.tsx exists as the credentials entry point.
  key_links:
    - middleware.ts and API handlers use the same session/auth source.
    - lib/archive.ts and archive queries accept an authenticated user scope.
    - app/page.tsx and private routes redirect or block when the session is absent.
---

<objective>
Add the auth and access-control layer on top of the new durable foundation.

Purpose: Turn the dashboard into a private multi-user tool with explicit route protection, admin-managed account creation/invites, and user-scoped data access.
Output: Better Auth configuration, login flow, bootstrap admin path, middleware/API protection, and user-scoped archive persistence.
</objective>

<execution_context>
@/Users/adi7192/Documents/Equity Intelligence/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adi7192/Documents/Equity Intelligence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-access/01-CONTEXT.md
@.planning/phases/01-foundation-access/01-RESEARCH.md
@.planning/phases/01-foundation-access/01-01-PLAN.md
@lib/archive.ts
@app/page.tsx
@app/api/update-data/route.ts
@app/api/archives/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Better Auth and the auth-backed schema surface</name>
  <files>package.json, .env.example, lib/auth.ts, lib/db/schema.ts, app/api/auth/[...all]/route.ts</files>
  <read_first>package.json, .env.example, lib/db/schema.ts, .planning/phases/01-foundation-access/01-CONTEXT.md, .planning/phases/01-foundation-access/01-RESEARCH.md</read_first>
  <action>Add the `better-auth` dependency to `package.json`, extend `.env.example` with `BETTER_AUTH_SECRET=`, `BETTER_AUTH_URL=`, `BOOTSTRAP_ADMIN_EMAIL=`, and `BOOTSTRAP_ADMIN_PASSWORD=`, create `lib/auth.ts` with Better Auth configured for email/password only, create exported helpers for loading the current session and enforcing a required session, extend `lib/db/schema.ts` with the auth/session tables that Better Auth needs plus an explicit user-role field and an `ownerUserId` linkage path for user-owned records, and create `app/api/auth/[...all]/route.ts` that wires the Better Auth handler into the Next.js App Router.</action>
  <verify>rg "better-auth" package.json && rg "^BETTER_AUTH_SECRET=|^BETTER_AUTH_URL=|^BOOTSTRAP_ADMIN_EMAIL=|^BOOTSTRAP_ADMIN_PASSWORD=" .env.example && test -f lib/auth.ts && test -f app/api/auth/[...all]/route.ts</verify>
  <acceptance_criteria>
    - `package.json` contains `better-auth`
    - `.env.example` contains `BETTER_AUTH_SECRET=`, `BETTER_AUTH_URL=`, `BOOTSTRAP_ADMIN_EMAIL=`, and `BOOTSTRAP_ADMIN_PASSWORD=`
    - `lib/auth.ts` contains exported auth/session helpers
    - `app/api/auth/[...all]/route.ts` exists and references the auth handler from `lib/auth.ts`
  </acceptance_criteria>
  <done>The project has a concrete Better Auth configuration and App Router auth endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Add the login/admin entry point and protect private pages and APIs</name>
  <files>middleware.ts, app/login/page.tsx, app/page.tsx, app/api/update-data/route.ts, app/api/archives/route.ts</files>
  <read_first>app/page.tsx, app/api/update-data/route.ts, app/api/archives/route.ts, lib/auth.ts, .planning/phases/01-foundation-access/01-CONTEXT.md</read_first>
  <action>Create `app/login/page.tsx` as the credentials entry point for the private app, include clear admin-only account-management messaging rather than public-signup copy, create `middleware.ts` that keeps `/login` and `/api/auth` public while redirecting unauthenticated requests away from private dashboard routes, and update `app/page.tsx`, `app/api/update-data/route.ts`, and `app/api/archives/route.ts` so they enforce a required session instead of assuming public access. Use explicit `401` or redirect behavior rather than silent fallthrough.</action>
  <verify>test -f middleware.ts && test -f app/login/page.tsx && rg "/login|/api/auth" middleware.ts && rg "401|Unauthorized|getRequiredSession" app/api/update-data/route.ts app/api/archives/route.ts && rg "admin|invite|account" app/login/page.tsx</verify>
  <acceptance_criteria>
    - `middleware.ts` exists and references `/login` and `/api/auth`
    - `app/login/page.tsx` exists
    - `app/api/update-data/route.ts` contains an unauthorized guard
    - `app/api/archives/route.ts` contains an unauthorized guard
    - `app/login/page.tsx` does not present the app as a public self-serve signup flow
  </acceptance_criteria>
  <done>Private routes and APIs now require a real authenticated session.</done>
</task>

<task type="auto">
  <name>Task 3: Scope persisted dashboard and archive data to the authenticated user</name>
  <files>lib/archive.ts, lib/db/queries/archive.ts, app/page.tsx, app/api/update-data/route.ts, app/api/archives/route.ts</files>
  <read_first>lib/archive.ts, lib/db/queries/archive.ts, lib/auth.ts, app/page.tsx, app/api/update-data/route.ts, app/api/archives/route.ts</read_first>
  <action>Update the archive persistence helpers so every live-state and archive read/write is scoped by an explicit authenticated user ID. Thread the current session's user ID from `app/page.tsx`, `app/api/update-data/route.ts`, and `app/api/archives/route.ts` into the archive query path, ensure archive listing and retrieval only return records owned by that user, and remove any remaining assumption that there is one global dashboard state shared by all users.</action>
  <verify>rg "userId|ownerUserId" lib/archive.ts lib/db/queries/archive.ts app/page.tsx app/api/update-data/route.ts app/api/archives/route.ts</verify>
  <acceptance_criteria>
    - `lib/db/queries/archive.ts` contains user-scoped filtering via `userId` or `ownerUserId`
    - `app/page.tsx` passes an authenticated user scope into the live-data load path
    - `app/api/update-data/route.ts` passes an authenticated user scope into save/archive operations
    - `app/api/archives/route.ts` returns only user-scoped archive data
  </acceptance_criteria>
  <done>Archive and dashboard persistence are scoped to the authenticated user rather than global state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `test -f lib/auth.ts && test -f app/api/auth/[...all]/route.ts && test -f middleware.ts && test -f app/login/page.tsx`
- [ ] `rg "Unauthorized|401|redirect" app/api/update-data/route.ts app/api/archives/route.ts middleware.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Only authenticated users can access the dashboard and private API surfaces
- Persisted archive and live dashboard data are scoped to the signed-in user
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-access/01-02-SUMMARY.md`
</output>
