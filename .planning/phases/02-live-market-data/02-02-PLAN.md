---
phase: 02-live-market-data
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - app/page.tsx
  - components/dashboard-client.tsx
  - components/market-header.tsx
  - components/watch-next-week.tsx
  - components/live-watchlist.tsx
  - components/live-market-status.tsx
  - app/admin/page.tsx
  - lib/live-market/contracts.ts
  - lib/db/queries/shared-watchlist.ts
  - app/api/market/snapshot/route.ts
  - app/api/market/stream/route.ts
autonomous: true
requirements:
  - LIVE-02
  - LIVE-03
user_setup:
  - Start the relay and provide working live-market env vars before validating the dashboard.
  - Ensure the admin account exists so the shared watchlist management path can be tested end-to-end.
must_haves:
  truths:
    - Signed-in users can see a live four-benchmark market overview with explicit freshness and market-closed state.
    - Signed-in users can see the shared starter watchlist with live last price, percent change, and volume.
    - Only admins can add or remove shared watchlist entries from inside the app.
    - The live market layer is visually and semantically distinct from the weekly digest/archive layer.
  artifacts:
    - components/market-header.tsx renders the real Phase 2 benchmark set and market status.
    - components/live-watchlist.tsx exists as the Phase 2 live watchlist surface.
    - app/admin/page.tsx contains minimal shared-watchlist management controls.
  key_links:
    - app/page.tsx passes relay-backed market data into the dashboard shell.
    - components/dashboard-client.tsx and app/api/market/* agree on live payload shape.
    - app/admin/page.tsx mutates the same shared-watchlist persistence layer that the dashboard consumes.
---

<objective>
Ship the Phase 2 product surfaces on top of the new live-market foundation.

Purpose: Replace the stale sample market overview/watchlist experience with authenticated live benchmark and watchlist UI, and give the admin a minimal in-app way to manage the shared starter watchlist.
Output: Updated dashboard header, live watchlist tab, explicit market-status/freshness UI, and admin-only shared-watchlist management.
</objective>

<execution_context>
@/Users/adi7192/Documents/Equity Intelligence/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adi7192/Documents/Equity Intelligence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-live-market-data/02-CONTEXT.md
@.planning/phases/02-live-market-data/02-RESEARCH.md
@.planning/phases/02-live-market-data/02-01-PLAN.md
@app/page.tsx
@components/dashboard-client.tsx
@components/market-header.tsx
@components/watch-next-week.tsx
@app/admin/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the sample market header with a live benchmark overview and explicit market status</name>
  <files>app/page.tsx, components/market-header.tsx, components/live-market-status.tsx, lib/live-market/contracts.ts</files>
  <read_first>app/page.tsx, components/market-header.tsx, lib/live-market/contracts.ts, .planning/phases/02-live-market-data/02-CONTEXT.md</read_first>
  <action>Update `app/page.tsx` so the dashboard loads the Phase 2 market snapshot path instead of treating the old `marketBaseline` sample as the authoritative header source, refactor `components/market-header.tsx` to render only the four locked Phase 2 benchmarks (Nifty 50, Sensex, Nifty Bank, Midcap 150), create `components/live-market-status.tsx` for freshness, last-updated, and market-state messaging, and adjust the surrounding dashboard copy so the top surface is clearly the live-market layer while the existing weekly digest/archive experience remains a separate analysis layer beneath it. Ensure the header can distinguish `closed`, `stale while open`, and `disconnected` states without falling back silently to the old sample baseline.</action>
  <verify>test -f components/live-market-status.tsx && rg "Sensex|Nifty Bank|Midcap 150|market closed|disconnected|last updated|fresh|weekly intelligence|live market" app/page.tsx components/market-header.tsx components/live-market-status.tsx components/dashboard-client.tsx</verify>
  <acceptance_criteria>
    - `components/market-header.tsx` references the four locked Phase 2 benchmarks and not Brent/INR/G-Sec
    - `components/live-market-status.tsx` exists
    - The header UI exposes freshness or last-updated information
    - The UI distinguishes market-closed, stale, and disconnected states instead of flattening them together
    - The live header is not still framed as a weekly digest-only surface
  </acceptance_criteria>
  <done>The dashboard header now reflects real Phase 2 live-market scope instead of the old sample baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Ship the live shared watchlist surface inside the existing dashboard shell</name>
  <files>components/dashboard-client.tsx, components/watch-next-week.tsx, components/live-watchlist.tsx, app/api/market/snapshot/route.ts, app/api/market/stream/route.ts</files>
  <read_first>components/dashboard-client.tsx, components/watch-next-week.tsx, app/api/market/snapshot/route.ts, app/api/market/stream/route.ts, .planning/phases/02-live-market-data/02-RESEARCH.md</read_first>
  <action>Create `components/live-watchlist.tsx` as the Phase 2 watchlist surface showing last price, percent change, and volume for the shared starter list, update `components/dashboard-client.tsx` so the watchlist tab consumes the `snapshot + SSE` market flow rather than the old narrative "watch next week" data, and either retire or repurpose `components/watch-next-week.tsx` so the tab no longer mixes event-calendar content with live watchlist rows. Reuse the current dashboard shell where it still fits, but make the live watchlist unmistakably a tracked-instruments panel and remove silent sample fallback from authenticated live-market surfaces.</action>
  <verify>test -f components/live-watchlist.tsx && rg "price|change|volume|EventSource|stream|snapshot|heartbeat|stale|disconnected" components/dashboard-client.tsx components/live-watchlist.tsx components/watch-next-week.tsx</verify>
  <acceptance_criteria>
    - `components/live-watchlist.tsx` exists and renders price, change, and volume
    - `components/dashboard-client.tsx` consumes the Phase 2 live watchlist shape
    - The watchlist tab no longer presents itself as a purely weekly/event narrative panel
    - The live watchlist handles loading, stale, and disconnected states gracefully
    - Authenticated live-market surfaces do not silently substitute old sample market/watchlist content when the relay is unavailable
  </acceptance_criteria>
  <done>The dashboard now has a real live watchlist surface tied to the Phase 2 market routes.</done>
</task>

<task type="auto">
  <name>Task 3: Add admin-only shared watchlist management inside the app</name>
  <files>app/admin/page.tsx, lib/db/queries/shared-watchlist.ts, app/api/market/snapshot/route.ts</files>
  <read_first>app/admin/page.tsx, lib/auth.ts, lib/db/queries/shared-watchlist.ts, .planning/phases/02-live-market-data/02-CONTEXT.md</read_first>
  <action>Extend `app/admin/page.tsx` with minimal controls for adding and removing entries from the shared starter watchlist, use the existing admin session boundary rather than introducing a new role system, wire those controls to the shared-watchlist persistence helpers from Plan `02-01`, and ensure non-admin users still cannot mutate or discover admin management flows. Keep the admin UX deliberately small but relay-ready: `exchange`, `symbol`, `displayName`, `instrumentToken`, `sortOrder`, optional `notes`, and remove action are sufficient for this phase. Shared watchlist mutations should also trigger the relay subscription-sync path so changes affect the live feed set quickly.</action>
  <verify>rg "watchlist|shared|add|remove|instrumentToken|exchange|sortOrder" app/admin/page.tsx && rg "hasAdminRole" app/admin/page.tsx</verify>
  <acceptance_criteria>
    - `app/admin/page.tsx` contains shared-watchlist management controls
    - Admin-only checks guard the watchlist management actions
    - Shared watchlist mutations reuse the same persistence layer consumed by the dashboard
    - Shared watchlist mutations update the relay subscription set without requiring a manual service restart
    - Non-admin users remain blocked from the admin route and cannot mutate the shared list
  </acceptance_criteria>
  <done>The admin can manage the shared starter watchlist from inside the app without expanding Phase 2 into full per-user watchlist CRUD.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npx tsc --noEmit` passes
- [ ] Signed-in users can see the live market overview and shared watchlist
- [ ] Non-admin users remain blocked from `/admin`
- [ ] Admin can add and remove a shared watchlist entry and the dashboard reflects it without a relay restart
- [ ] Market-open, market-closed, stale, and disconnected states are each visibly distinguishable in the UI
- [ ] The old sample market/watchlist content cannot silently reappear in authenticated live-market surfaces
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The dashboard visibly communicates live-vs-closed state and no longer depends on the old sample market header shape
- The shared starter watchlist is visible to signed-in users and manageable by the admin only
  </success_criteria>

<output>
After completion, create `.planning/phases/02-live-market-data/02-02-SUMMARY.md`
</output>
