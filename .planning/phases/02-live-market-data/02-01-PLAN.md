---
phase: 02-live-market-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .env.example
  - lib/db/schema.ts
  - lib/db/queries/shared-watchlist.ts
  - lib/live-market/contracts.ts
  - lib/live-market/instruments.ts
  - lib/live-market/relay-client.ts
  - app/api/market/snapshot/route.ts
  - app/api/market/stream/route.ts
  - relay/package.json
  - relay/tsconfig.json
  - relay/src/index.ts
  - relay/src/kite.ts
  - relay/src/store.ts
  - relay/README.md
autonomous: true
requirements:
  - LIVE-01
  - LIVE-02
  - LIVE-03
user_setup:
  - Create or reuse a Zerodha Kite developer app and provide working relay credentials such as `KITE_API_KEY`, a fresh `KITE_ACCESS_TOKEN`, `LIVE_RELAY_URL`, and `LIVE_RELAY_SECRET`.
  - Document and follow a daily Kite access-token refresh workflow for the relay, even if the first version is manual.
  - Run or deploy the relay as a separate always-on process outside the main Vercel app.
must_haves:
  truths:
    - A separate relay process can ingest Kite prices for the tracked benchmark and shared-watchlist instruments during market hours.
    - The main app has an authenticated hybrid market contract: snapshot for first paint/recovery and SSE for near-live updates during open sessions.
    - The shared starter watchlist is stored durably and can be reused by the dashboard and the admin UI.
  artifacts:
    - lib/live-market/contracts.ts defines the payload shape shared by relay and app.
    - lib/live-market/relay-client.ts reads authenticated data from the relay.
    - app/api/market/snapshot/route.ts and app/api/market/stream/route.ts exist as app-facing market routes.
    - relay/ contains a runnable standalone live-market service.
    - lib/db/queries/shared-watchlist.ts exists as the shared watchlist persistence layer.
  key_links:
    - relay/src/store.ts and lib/live-market/contracts.ts agree on payload shape.
    - app/api/market/* routes authenticate through the existing Phase 1 session boundary before returning market data.
    - shared watchlist persistence links tracked symbols/instrument metadata to the relay subscription set.
    - admin watchlist mutations trigger an explicit relay subscription-sync path instead of relying on service restarts.
---

<objective>
Establish the server-side live-market foundation for the app.

Purpose: Build the durable shared-watchlist data model, the authenticated app-facing market routes, and the separate always-on Kite relay needed to feed the dashboard during market hours.
Output: Relay scaffolding, live-market contracts, shared watchlist persistence, and authenticated snapshot/stream routes inside the Next.js app.
</objective>

<execution_context>
@/Users/adi7192/Documents/Equity Intelligence/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adi7192/Documents/Equity Intelligence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-live-market-data/02-CONTEXT.md
@.planning/phases/02-live-market-data/02-RESEARCH.md
@.planning/phases/01-foundation-access/01-02-SUMMARY.md
@lib/db/schema.ts
@app/page.tsx
@components/market-header.tsx
@app/admin/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add live-market dependencies, env scaffolding, and shared contracts</name>
  <files>package.json, .env.example, lib/live-market/contracts.ts, lib/live-market/instruments.ts, lib/live-market/relay-client.ts</files>
  <read_first>package.json, .env.example, .planning/phases/02-live-market-data/02-CONTEXT.md, .planning/phases/02-live-market-data/02-RESEARCH.md, .planning/research/ARCHITECTURE.md</read_first>
  <action>Add the Phase 2 live-market dependency surface in `package.json` (at minimum `kiteconnect` if not already present), extend `.env.example` with the relay-facing environment variables needed by the app and the sidecar service, create `lib/live-market/contracts.ts` with typed `snapshot + SSE` contracts including sequence/version, market phase, freshness state, connection state, and benchmark/watchlist payloads, create `lib/live-market/instruments.ts` with the four benchmark identifiers and starter watchlist seed metadata shape, and create `lib/live-market/relay-client.ts` with authenticated server-side fetch helpers from the Next app to the relay.</action>
  <verify>rg "kiteconnect" package.json && rg "^KITE_API_KEY=|^KITE_ACCESS_TOKEN=|^LIVE_RELAY_URL=|^LIVE_RELAY_SECRET=" .env.example && test -f lib/live-market/contracts.ts && test -f lib/live-market/instruments.ts && test -f lib/live-market/relay-client.ts</verify>
  <acceptance_criteria>
    - `package.json` contains `kiteconnect`
    - `.env.example` contains `KITE_API_KEY=`, `KITE_ACCESS_TOKEN=`, `LIVE_RELAY_URL=`, and `LIVE_RELAY_SECRET=`
    - `lib/live-market/contracts.ts` defines snapshot and SSE payload types including market/freshness/connection metadata
    - `lib/live-market/instruments.ts` references Nifty 50, Sensex, Nifty Bank, and Midcap 150
    - `lib/live-market/relay-client.ts` sends authenticated server-side requests to the relay
  </acceptance_criteria>
  <done>The project has the core dependency, config, and typed relay contract surface needed for live-market work.</done>
</task>

<task type="auto">
  <name>Task 2: Create the shared starter watchlist persistence layer and app-facing market routes</name>
  <files>lib/db/schema.ts, lib/db/queries/shared-watchlist.ts, app/api/market/snapshot/route.ts, app/api/market/stream/route.ts</files>
  <read_first>lib/db/schema.ts, lib/auth.ts, app/api/update-data/route.ts, .planning/REQUIREMENTS.md, .planning/phases/02-live-market-data/02-CONTEXT.md</read_first>
  <action>Extend `lib/db/schema.ts` with an explicit `shared_watchlist_items` table for the admin-managed starter list rather than overloading per-user watchlists, store relay-ready fields such as `exchange`, `symbol`, `displayName`, `instrumentToken`, and `sortOrder`, create `lib/db/queries/shared-watchlist.ts` with helpers for listing, adding, removing, and ordering shared watchlist entries, create `app/api/market/snapshot/route.ts` as an authenticated JSON route that returns the latest relay-backed benchmark/watchlist snapshot to signed-in users, and create `app/api/market/stream/route.ts` as the authenticated SSE path the dashboard will consume for `snapshot`, `quotes`, `status`, and `heartbeat` events.</action>
  <verify>rg "shared_watchlist_items|instrument_token|sort_order" lib/db/schema.ts && test -f lib/db/queries/shared-watchlist.ts && test -f app/api/market/snapshot/route.ts && test -f app/api/market/stream/route.ts && rg "getRequiredSession|heartbeat|snapshot|status|quotes" app/api/market/snapshot/route.ts app/api/market/stream/route.ts</verify>
  <acceptance_criteria>
    - `lib/db/schema.ts` contains an explicit `shared_watchlist_items` table, not a reuse of `watchlists` / `watchlist_items`
    - `lib/db/queries/shared-watchlist.ts` exports list/add/remove/reorder-style helpers
    - `app/api/market/snapshot/route.ts` exists and requires authentication
    - `app/api/market/stream/route.ts` exists and requires authentication
    - The app-facing market routes return freshness, market, and connection metadata, not just raw prices
    - The stored shared-watchlist shape is relay-ready without depending on `stocks.id`
  </acceptance_criteria>
  <done>The app has a durable shared-watchlist source of truth and authenticated market-data routes ready for the UI.</done>
</task>

<task type="auto">
  <name>Task 3: Build the separate Kite relay service with latest-tick caching and subscription sync</name>
  <files>relay/package.json, relay/tsconfig.json, relay/src/index.ts, relay/src/kite.ts, relay/src/store.ts, relay/README.md</files>
  <read_first>.planning/phases/02-live-market-data/02-RESEARCH.md, .planning/research/ARCHITECTURE.md, lib/live-market/contracts.ts, lib/live-market/instruments.ts</read_first>
  <action>Create a small standalone `relay/` service inside the repo with its own `package.json` and `tsconfig.json`, implement `relay/src/kite.ts` for the KiteTicker connection and instrument subscription lifecycle, implement `relay/src/store.ts` as the latest-tick cache plus freshness metadata store, implement `relay/src/index.ts` with a minimal authenticated HTTP surface for `GET /health`, `GET /snapshot`, relay event streaming, and `POST /subscriptions/sync`, and document local/dev startup, token-refresh expectations, and operational recovery steps in `relay/README.md`. Keep the relay narrow: upstream socket, latest state cache, service auth, subscription sync, and read endpoints only.</action>
  <verify>test -f relay/package.json && test -f relay/src/index.ts && test -f relay/src/kite.ts && test -f relay/src/store.ts && rg "KiteTicker|kiteconnect|snapshot|health|subscriptions|sync|heartbeat" relay/src/index.ts relay/src/kite.ts relay/src/store.ts</verify>
  <acceptance_criteria>
    - `relay/` exists as a standalone runnable service
    - `relay/src/kite.ts` references the Kite client/subscription flow
    - `relay/src/store.ts` contains latest-tick caching plus freshness metadata
    - `relay/src/index.ts` exposes authenticated snapshot/events endpoints, a health check, and a subscription-sync endpoint
    - `relay/README.md` explains how the relay is started separately from the main app and how the Kite access token is refreshed operationally
  </acceptance_criteria>
  <done>The repo contains a concrete always-on relay implementation instead of leaving Phase 2 as architecture only.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `test -f lib/live-market/contracts.ts && test -f lib/live-market/relay-client.ts && test -f app/api/market/snapshot/route.ts && test -f app/api/market/stream/route.ts`
- [ ] `test -f relay/package.json && test -f relay/src/index.ts && test -f relay/src/kite.ts && test -f relay/src/store.ts`
- [ ] Relay health, snapshot, and subscription-sync flows still work after a relay restart
- [ ] App stream route emits reconnect-friendly events and heartbeat traffic without silent mock fallback
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The main app has a clear authenticated read boundary for live market data
- A separate relay service exists and can own the upstream Kite connection plus latest-state cache
  </success_criteria>

<output>
After completion, create `.planning/phases/02-live-market-data/02-01-SUMMARY.md`
</output>
